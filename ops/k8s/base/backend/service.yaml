apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: datashare
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: api
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 7777
      targetPort: 7777
